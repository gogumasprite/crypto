{
    "name": "crypto-yield-factory",
    "version": "1.0.0",
    "scripts": {
        "build": "pip3 install -r requirements.txt && python3 scripts/fetch_data.py && python3 scripts/build_site.py"
    }
}